package com.example.bookreader.library.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Centralized color palette for the library UI.
 * All colors used in Composables should reference these tokens.
 * Update here to theme the entire app.
 */
object LibraryColorTokens {
    val Background = Color(0xFFFAF5EA)
    val PrimaryText = Color(0xFF1F1D1A)
    val SecondaryText = Color(0xFF6E675F)
    val Accent = Color(0xFF635847)
    val Divider = Color(0xFFE6DECF)
    val ProgressTrack = Color(0xFFEBE2D3)
}
